# Upward Reporting Skill 设计说明

> 这是项目的**设计文档**,讲清"为什么这么设计、怎么运转、怎么扩展"。
> 想快速了解"是什么、怎么用、怎么装",看 [README.md](README.md)(项目正门)。

`upward-reporting` 是一个用于职场向上汇报的 skill。它面向一种很常见的工作场景:用户脑子里有大量信息,甚至已经写了一大段材料,但内容还是散的,直接发给领导会像背景说明、流水账或项目文档。

这个 skill 的目标是:用户用日常自然语言输入,LLM 自动整理成更适合向上沟通的专业表达。

```text
自然语言材料 -> 专业汇报意图 -> 结论 + 证据 + 影响 + 建议 + 请求
```

## 解决什么痛点

很多向上汇报写不好,问题通常不在事实数量,而在信息没有被翻译成管理者关心的结构。

常见问题有:

- 只写“做了什么”,没有写“这件事意味着什么”。
- 背景很多,结论很晚才出现。
- 风险写得模糊,对方看不出影响范围和处理优先级。
- 诉求不明确,只说“希望支持”,没有说需要谁在什么时候做什么。
- 同一段内容给直属上级和给老板都用同一种说法。
- 数据分析师容易写成“取数、写 SQL、做看板”,没有说清楚分析支持了什么业务判断。
- 用户日常输入通常很口语,不会先写一个标准 prompt。

这个 skill 主要解决两件事:

1. 把散乱、口语、半成品材料转成专业汇报语言。
2. 根据受众、场景、职业角色自动选择不同表达口径。

## 设计原则

这个 skill 按几个原则设计:

- **自然语言优先**:用户可以直接写“这段怎么跟领导说”,不需要先写完整 prompt。
- **结论先行**:先给判断,再给背景和细节。
- **影响优先**:把“做了什么”转成“影响什么目标、风险或决策”。
- **诉求明确**:最后要能看出是否需要对方知情、拍板、协调、给资源或确认优先级。
- **受众分层**:给 +1 多讲过程和卡点,给老板多讲影响和决策。
- **受众由用户选择**:如果用户只说“领导”,skill 会先问是直属上级还是更高层老板,不替用户默认。
- **角色可扩展**:数据分析师、产品经理、运营、研发、销售等角色可以拆成独立模板。
- **不编事实**:可以专业化措辞,但不能补造指标、owner、日期或结论。
- **结论强度 = 证据强度**(数据岗底线):相关不写成因果,没过显著不写成“有效”。这是后期重点打磨的一层,详见下文“数据分析师版怎么设计”。

## 整体架构

它采用路由式结构:先判断输入,再选择合适的汇报口径。

```text
自然语言入口
  -> 任务类型识别
  -> 受众模式选择
  -> 场景模板选择
  -> 职业角色选择
  -> 渠道/格式选择
  -> 输出质量检查
```

更具体地说:

```text
用户原话
-> 提取事实、判断、风险、隐含诉求
-> 判断这是汇报、决策、风险升级、资源请求还是对齐预期
-> 判断是给 +1、老板、跨部门还是外部对象
-> 判断是周报、项目进展、分析结论、会议纪要还是路线图规划
-> 判断按数据分析师、产品经理或其他角色口径写
-> 判断渠道:微信/Slack、邮件、口头、一页纸,决定形态和长度
-> 生成可直接发送的汇报,并过一遍质量检查(结论先行、诉求明确、不编事实、相关≠因果)
```

受众、场景、角色、渠道是**四个正交维度**,分别选、再组合,少量规则就能覆盖绝大多数情况。

## 运行链路

### 1. 自然语言入口

用户可以写得很随意,例如:

```text
现在这个埋点东西有点乱,原来都在 Excel 和飞书里,字段含义也不清楚,后面想做搜索和知识图谱,这个怎么跟领导说?
```

skill 会先把这段话翻译成专业汇报意图:

```text
事实:埋点定义分散在 Excel、飞书、需求文档等渠道。
问题:定义不统一、字段含义不清、owner 不明确、变更追溯困难。
方向:下一阶段准备建设搜索和知识图谱。
隐含诉求:需要向领导说明当前进展、平台价值、下一阶段优先级。
```

这一步由 `references/natural-language-intake.md` 负责。

### 2. 任务类型识别

skill 会判断用户到底想要哪类向上沟通:

```text
inform   -> 同步进展
decide   -> 请求决策
escalate -> 升级风险
ask      -> 请求资源/协调/授权
align    -> 对齐预期和优先级
```

例如“这个怎么跟领导说”通常是 `inform + align`;
“要不要推进这个方案,帮我写给老板”通常是 `decide`;
“这个风险要升级一下”通常是 `escalate`。

### 3. 受众模式选择

同一件事,给不同人看,写法不同。

如果用户只说“领导”,skill 不会直接默认成 +1。它会先问:

```text
是给直属上级（+1），还是给更高层老板/大老板？
```

用户选完以后再生成对应版本。如果用户想两个版本都看,可以直接说“两个版本都给我”。

`向+1汇报` 更关注:

```text
当前进展
我的判断
卡点和依赖
下一步
需要直属上级怎么帮
```

`向老板汇报` 更关注:

```text
业务影响
关键事实
风险和代价
建议方案
需要老板拍板什么
```

这部分由 `references/audience-modes.md` 负责。

### 4. 场景模板选择

不同汇报场景有不同骨架。

目前支持:

- 周报 / 项目进展
- 风险升级
- 资源请求
- 决策请求
- 老板一页纸
- 会议结论
- 分析结论
- 分析进展
- 上线 / 交付更新

这部分由 `references/scenario-templates.md` 负责。

### 5. 职业角色选择

同样是向上汇报,数据分析师和产品经理关注点不一样。

数据分析师版强调:

```text
业务问题
结论
关键依据
可信度与限制
业务影响
建议动作
需要支持
```

产品经理版强调:

```text
用户/业务问题
产品判断
关键证据
范围与取舍
跨部门依赖
上线或迭代风险
需要拍板
```

这部分由角色文件负责:

```text
references/roles/data-analyst.md
references/roles/product-manager.md
```

未来新增运营、研发、销售、市场等角色时,也按这个模式继续往 `references/roles/` 里拆文件。

### 6. 渠道/格式与质量检查

最后按渠道决定形态和长度(微信/Slack 一屏、邮件带主题分段、口头 30 秒、一页纸高管版),并在输出前过一遍质量检查:结论是否先行、诉求是否可执行、有没有编事实、引用数据时有没有把相关当因果或把不显著说成有效。这部分由 `SKILL.md` 的 Medium And Length 与 Quality Bar 负责。

## 文件结构

```text
upward-reporting/
├── SKILL.md                          # 入口与路由中枢:路由流程、质量检查、渠道映射、输出约定
├── README.md                         # 项目正门(给人看:是什么、怎么用)
├── 设计说明.md                       # 本文件(设计文档:为什么、架构、扩展)
└── references/                       # 按需加载的深层细则
    ├── README.md                     # references 目录索引(按路由维度编目)
    ├── natural-language-intake.md    # 自然语言理解:口语 -> 专业汇报意图
    ├── audience-modes.md             # 受众口径:+1 / 老板 / 跨部门 / 外部
    ├── scenario-templates.md         # 场景模板:周报/风险/资源/决策/一页纸/纪要/上线…
    ├── templates.md                  # 短格式模板(微信/邮件/口头)+ 改写规则 + 语气变体
    ├── examples.md                   # 端到端范例(乱素材->成稿),含数据严谨的正反对照
    ├── source-patterns.md            # 方法论来源(结论先行 / PPP / 项目状态报告 / 老板汇报偏好)
    └── roles/                        # 角色口径(可扩展)
        ├── index.md                  # 角色索引与扩展规范
        ├── data-analyst.md           # 数据分析师模板(含 Causal Rigor)
        └── product-manager.md        # 产品经理模板
```

## 文件分工

`SKILL.md` 是入口和路由中枢。它负责告诉 agent 什么时候使用这个 skill、按什么顺序判断、需要读取哪些 reference 文件、最终输出要满足什么质量标准。

`references/README.md` 是参考目录的索引,把各文件按“口语归一 -> 受众 -> 场景 -> 角色 -> 格式”编目,方便快速定位。

`natural-language-intake.md` 负责自然语言理解,把口语、碎片笔记、半成品文档转成专业汇报意图。

`audience-modes.md` 负责受众口径,包括向 +1、向老板、跨部门负责人、客户/外部合作方。

`scenario-templates.md` 负责场景模板,包括周报、风险升级、资源请求、决策请求、分析结论、上线更新等。

`templates.md` 放短格式模板和改写规则,例如微信/Slack、邮件、30 秒口播。

`examples.md` 放可直接照抄的端到端范例,包含显著性、因果两个最容易翻车场景的正反对照。

`roles/index.md` 是角色索引,用来说明已有角色和新增角色的规范。

`roles/data-analyst.md` 是数据分析师模板,并承载显著性与因果纪律(Causal Rigor),是这条纪律的单一事实源,其它角色引用它。

`roles/product-manager.md` 是产品经理模板。

`source-patterns.md` 放背后的方法论来源,例如结论先行、PPP、项目状态报告、老板汇报偏好等。

## 数据分析师版怎么设计

数据分析师最容易写成“我做了很多分析工作”。这个 skill 会把它改成“这个分析回答了什么业务问题”。

它的默认结构是:

```text
业务问题
结论
关键依据
可信度与限制
业务影响
建议动作
需要支持
```

例子:

```text
结论:从近 7 天数据看,新用户首单转化下降主要集中在渠道 A,暂时不像整体产品流程问题。建议先暂停渠道 A 增量投放,同时补做落地页路径分析。

关键依据:
- 新用户首单转化率从 18.4% 降到 14.9%,下降 3.5 个百分点。
- 渠道 A 转化率从 16.1% 降到 9.8%,其他渠道波动在 1 个百分点以内。
- 漏斗拆分显示主要流失发生在落地页到注册环节,不是支付环节。

可信度:目前是方向性判断。渠道归因口径已确认,但落地页按钮点击埋点缺失,还需要补验证具体流失原因。

建议动作:本周先暂停渠道 A 增量预算。我会在周五前补充路径分析,给出是否恢复投放的建议。
```

在数据结论上,这个 skill 有一条硬纪律:**结论强度只能等于证据强度**。

- **相关 ≠ 因果**:分群对比、同向涨跌只能说“相关”;要下因果,得有 A/B 实验,或 DiD / RDD / IV / PSM 等因果推断设计,并写明识别假设。
- **没显著 ≠ 有效,也 ≠ 没效果**:没过显著只能说“未测出差异”,不能说成“有提升”;也不能反推“效果一定小”——那要看 MDE / 检验功效,区分不了就标“待确认”。

完整规则见 `references/roles/data-analyst.md` 的 Causal Rigor 一节,正反对照范例见 `references/examples.md` 的 Example 4(显著性)、Example 5(因果)。

## 后续扩展方式

新增角色时,在 `references/roles/` 下创建独立文件:

```text
references/roles/operations.md
references/roles/engineer.md
references/roles/sales.md
references/roles/marketing.md
```

每个角色文件保持同一套骨架:

```text
Core Structure
What To Emphasize
Avoid
向+1汇报 Pattern
向老板汇报 Pattern
Common Scenarios
```

然后在 `references/roles/index.md` 里加一行索引。

这样做的好处是:角色越加越多,`SKILL.md` 仍然只负责路由,不会变成难维护的模板合集;而像显著性、因果这类跨角色的通用纪律,只写在 `data-analyst.md` 一处,其它角色引用它,不重复也不会改漏。

## 使用与安装

日常怎么触发、怎么注册到 Claude Code、怎么测试,见 [README.md](README.md) 的“快速上手”。本文件只负责设计层面的说明。
